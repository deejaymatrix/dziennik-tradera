// Własny, mały protokół synchronizacji offline-first (outbox, push/pull, konflikty)
// - patrz docs/specyfikacja-produktu.md §6 i ADR-0002 w docs/decyzje-architektoniczne.md.
// Status: pakiet zarezerwowany w strukturze monorepo (Kamień 0).
// Implementacja i testy kontraktowe powstają w Kamieniu 2.
export {};
