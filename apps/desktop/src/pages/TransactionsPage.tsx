import type { ReactElement } from "react";
import { PlaceholderPage } from "./PlaceholderPage";

export function TransactionsPage(): ReactElement {
  return (
    <PlaceholderPage
      description="Tabela transakcji z filtrowaniem, sortowaniem i formularzem dodawania pozycji."
      targetMilestone="Celu 1.5 i 1.6"
    />
  );
}
