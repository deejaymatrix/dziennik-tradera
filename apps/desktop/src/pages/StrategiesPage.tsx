import type { ReactElement } from "react";
import { PlaceholderPage } from "./PlaceholderPage";

export function StrategiesPage(): ReactElement {
  return (
    <PlaceholderPage
      description="Lista strategii startuje pusta - użytkownik tworzy, edytuje, duplikuje i archiwizuje własne."
      targetMilestone="Celu 1.5"
    />
  );
}
